export type NewsItem = {
    image_url: string,
    title: string,
    date: string,
    description: string,
};

export type FacultyItem = {
    title: string,
    image_url: string,
    description: string,
    detail_url: string,
};

export type ServiceItem = {
    title: string,
    description: string,
    url: string,
};

export type StatisticItem = {
    title: string,
    description: string,
    total: number,
};
